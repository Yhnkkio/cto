from __future__ import annotations

# Placeholder for host-server (5037) implementation.
# A future iteration will implement the host: service multiplexer and routing
# toward device sessions. For now, device mode is supported via direct
# TCP connections on the configured port (default 5555).
